services:
  - type: web
    name: hw9-movie-crawler-api
    runtime: python
    buildCommand: pip install -r api/requirements.txt
    startCommand: gunicorn api.index:app --bind 0.0.0.0:$PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false
